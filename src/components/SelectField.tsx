import React, {useState} from 'react';
import {Modal, Pressable, ScrollView, StyleSheet, Text, View} from 'react-native';

type Option = {id: string; name: string};
type Props = {label: string; value: string; options: Option[]; onChange: (value: string) => void};
export function SelectField({label, value, options, onChange}: Props) { const [open, setOpen] = useState(false); return <View style={styles.field}><Text style={styles.label}>{label}</Text><Pressable style={styles.select} onPress={() => setOpen(true)}><Text style={styles.value}>{options.find(option => option.id === value)?.name || 'Choose an option'}</Text><Text style={styles.chevron}>⌄</Text></Pressable><Modal visible={open} transparent animationType="slide" onRequestClose={() => setOpen(false)}><View style={styles.backdrop}><View style={styles.sheet}><Text style={styles.title}>{label}</Text><ScrollView>{options.map(option => <Pressable key={option.id} style={styles.option} onPress={() => {onChange(option.id); setOpen(false);}}><Text style={styles.optionText}>{option.name}</Text></Pressable>)}</ScrollView><Pressable onPress={() => setOpen(false)} style={styles.cancel}><Text style={styles.cancelText}>Cancel</Text></Pressable></View></View></Modal></View>; }
const styles = StyleSheet.create({field: {marginBottom: 15}, label: {fontWeight: '800', color: '#4e596b', marginBottom: 7}, select: {backgroundColor: '#fff', borderWidth: 1, borderColor: '#dfe4ed', borderRadius: 10, padding: 14, flexDirection: 'row', justifyContent: 'space-between'}, value: {color: '#172033', flex: 1}, chevron: {color: '#5267d9', fontSize: 18}, backdrop: {flex: 1, justifyContent: 'flex-end', backgroundColor: 'rgba(23,32,51,0.35)'}, sheet: {backgroundColor: '#fff', borderTopLeftRadius: 20, borderTopRightRadius: 20, padding: 20, maxHeight: '75%'}, title: {fontSize: 20, fontWeight: '800', color: '#172033', marginBottom: 12}, option: {paddingVertical: 15, borderBottomWidth: 1, borderBottomColor: '#edf0f4'}, optionText: {fontSize: 16, color: '#172033'}, cancel: {padding: 15, alignItems: 'center'}, cancelText: {color: '#5267d9', fontWeight: '800'}});
